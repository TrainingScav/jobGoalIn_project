{{> layout/header}}
<body>
<div class="main">
    <!-- ✅ 가운데 정렬된 탭 메뉴 -->
    <div class="tabs justify-content-center">
        <div class="tab active">MY</div>
        <a class="tab" href="update-form">회원정보</a>
        <a class="tab" href="resume-list">이력서</a>
    </div>

    <div class="profile-box">
        <!-- ✅ 프로필 이미지 제거 -->
        <div class="profile-info">
            <h3>{{userInfo.userNickName}}</h3>
            <div class="meta">가입일:{{userInfo.userCreatedAt}}</div>
            <div class="position">백엔드 개발자</div>

            <div class="skill-tags">
                {{#userSkillList}}
                <span class="skill-tag">{{skillList.skillName}}</span>
                {{/userSkillList}}
            </div>
        </div>
    </div>
</div>
</body>
{{> layout/footer}}
